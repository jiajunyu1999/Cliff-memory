# Data

Datasets and pretrained representations are intentionally not committed to the
repository. They can be large, licensed, or generated from external sources.

Place local assets under this directory and run `PYTHONPATH=src python
scripts/audit_assets.py` to check the expected layout. Do not commit API keys,
credentials, raw private data, model checkpoints, or generated experiment
outputs.
